#Image Blur Two

#Give all the contents within the outer array a name/address/index for reference then 
  #Give all contents within each inner array a name/address/index for refernece then 
    # Locate if there are any 1's present within any of the inner arrays then 
      #Create a sequence that will target and mutate the integer above, below, to the right, and to the left of any found 1's
        